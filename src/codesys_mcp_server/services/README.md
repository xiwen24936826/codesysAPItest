# Services

服务层按业务域拆分，每个模块只负责一类能力：

- `projects/`: 项目生命周期与控制器插入
- `pous/`: POU 创建与文本编辑
- `devices/`: 网络扫描与通信绑定
- `online/`: 在线登录与运行控制
- `ethercat/`: 预留，当前暂缓

后续实现时遵循以下原则：

- 每次只新增一个函数或一个模块
- 每个函数返回结构化 JSON 兼容对象
- 保留日志、异常处理和单元测试入口
